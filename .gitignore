# Instrucciones de contexto para asistentes IA — no pertenecen al repositorio
CLAUDE.md
.claude/
.cursor/
.cursorrules

# Artefactos de entorno local
.vscode/
*.code-workspace

# Datasheets y documentos de terceros (referenciar externamente)
docs/*.pdf

# PlatformIO — artefactos de build
firmware/.pio/

# Python — caché de las herramientas del firmware
firmware/tools/__pycache__/

# Dataset de audio — archivos WAV crudos (>10 MB, no versionar)
firmware/dataset/

# Python — entorno virtual y artefactos
servidor/.venv/
servidor/__pycache__/
servidor/**/__pycache__/
servidor/*.pyc
servidor/.env

# Credenciales y configuración local
*.env
secrets.h
config_local.h
servidor/set_hf_secrets.py
